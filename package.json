{
  "name": "aprs-internal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute aprs-internal-db --local --file=migrations/0001_initial.sql && wrangler d1 execute aprs-internal-db --local --file=migrations/0002_seed.sql && wrangler d1 execute aprs-internal-db --local --file=migrations/0003_kanban.sql",
    "db:migrate:remote": "wrangler d1 execute aprs-internal-db --remote --file=migrations/0001_initial.sql && wrangler d1 execute aprs-internal-db --remote --file=migrations/0002_seed.sql && wrangler d1 execute aprs-internal-db --remote --file=migrations/0003_kanban.sql",
    "db:seed:local": "wrangler d1 execute aprs-internal-db --local --file=migrations/0002_seed.sql",
    "db:seed:remote": "wrangler d1 execute aprs-internal-db --remote --file=migrations/0002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
